SUPABASE_URL=https://sneadexnpdyazrfgkkod.supabase.co
SUPABASE_ANON_KEY=<paste anon key from Supabase dashboard → Project Settings → API>

# Credentials for the provisioned test user in auth.users. Create a dedicated
# account for verification — do NOT reuse your primary RailPrep login.
VERIFY_EMAIL=<email>
VERIFY_PASSWORD=<password>
